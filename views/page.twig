{% extends "base.twig" %}

{% block content %}
{# <section id="page-hero" class="page-header bg-black px-2 lg:px-8">
		<div class="container mx-auto flex flex-col justify-center hero-background animate__animated animate__fadeInDown" style="background-image: url('{{post.thumbnail.src}}'); background-repeat: no-repeat; background-position: center; background-size: cover;">
			<div class="ps-8">
				<h1 class="text-6xl text-white uppercase font-extrabold w-10/12">{{post.title}}</h1>
			</div>
		</div>
	</section> #}
	{% include "page-header.twig" %}
	<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
		<section class="article-content">
			<div id="post-content" class="container mx-auto px-2 lg:px-8">
				{{post.content}}
			</div>
		</section>
	</article>
{% endblock %}